# Configurations Package

This package is the canonical import path for transport mappings and run configuration.

It re-exports the legacy `codebase/config` modules so existing code keeps working
while new imports move to `configurations.*`.

