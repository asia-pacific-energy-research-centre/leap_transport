"""Canonical configuration package for transport workflows.

This package re-exports the legacy `config` modules so new code can import
from `configurations` without changing the underlying source files yet.
"""

